diff --git a/third_party/electron_node/BUILD.gn b/third_party/electron_node/BUILD.gn
index 1ed186b..638557e 100644
--- a/third_party/electron_node/BUILD.gn
+++ b/third_party/electron_node/BUILD.gn
@@ -9,6 +9,14 @@
 # Please modify the gyp files if you are making changes to build system.
 
 import("unofficial.gni")
+import("//build/config/linux/pkg_config.gni")
 
+pkg_config("cares") {
+  packages = [ "libcares" ]
+}
+
+pkg_config("nghttp2") {
+  packages = [ "libnghttp2" ]
+}
 node_gn_build("node") {
 }
diff --git a/third_party/electron_node/unofficial.gni b/third_party/electron_node/unofficial.gni
index 71e8ac5..1707509 100644
--- a/third_party/electron_node/unofficial.gni
+++ b/third_party/electron_node/unofficial.gni
@@ -150,7 +150,9 @@ template("node_gn_build") {
   source_set("libnode") {
     configs += [
       ":node_internal_config",
-      "//build/config/compiler:no_exit_time_destructors"
+      "//build/config/compiler:no_exit_time_destructors",
+      ":cares",
+      ":nghttp2"
     ]
     public_configs = [
       ":node_external_config",
@@ -166,12 +167,10 @@ template("node_gn_build") {
     ]
     deps = [
       ":run_node_js2c",
-      "deps/cares",
       "deps/histogram",
       "deps/llhttp",
       "deps/merve",
       "deps/nbytes",
-      "deps/nghttp2",
       "deps/postject",
       "deps/uvwasi",
       "//third_party/zlib",
@@ -257,19 +256,6 @@ template("node_gn_build") {
       sources += node_inspector.node_inspector_sources +
                  node_inspector.node_inspector_generated_sources
     }
-    if (is_linux) {
-      import("//build/config/linux/pkg_config.gni")
-        if (use_system_cares) {
-          pkg_config("cares") {
-            packages = [ "libcares" ]
-          }
-        }
-      if (use_system_nghttp2) {
-        pkg_config("nghttp2") {
-          packages = [ "libnghttp2" ]
-        }
-      }
-    }
   }
 
   config("zstd_include_config") {
