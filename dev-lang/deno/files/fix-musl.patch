diff --git a/ext/node/ops/process.rs b/ext/node/ops/process.rs
index 2448dca80e..7eab4c159c 100644
--- a/ext/node/ops/process.rs
+++ b/ext/node/ops/process.rs
@@ -102,7 +102,7 @@ unsafe fn compute_argv_info(
   }
 }
 
-#[cfg(target_os = "linux")]
+#[cfg(all(target_os = "linux", target_env = "gnu"))]
 #[used]
 #[unsafe(link_section = ".init_array")]
 static ARGV_INIT_FN: unsafe extern "C" fn(
diff --git a/runtime/ops/worker_host.rs b/runtime/ops/worker_host.rs
index 049aecb602..4c013d4e19 100644
--- a/runtime/ops/worker_host.rs
+++ b/runtime/ops/worker_host.rs
@@ -436,7 +436,7 @@ fn op_create_worker(
     // OS. Without this, glibc in particular holds onto the fragmented heap
     // pages, causing RSS to remain high after many workers are created and
     // destroyed (https://github.com/denoland/deno/issues/26058).
-    #[cfg(target_os = "linux")]
+    #[cfg(all(target_os = "linux", target_env = "gnu"))]
     {
       // SAFETY: calling libc function with no preconditions.
       unsafe {
diff --git a/runtime/worker.rs b/runtime/worker.rs
index e8209fccd9..c66f047af2 100644
--- a/runtime/worker.rs
+++ b/runtime/worker.rs
@@ -80,6 +80,7 @@ pub(crate) static SIGUSR2_RX: LazyLock<tokio::sync::watch::Receiver<()>> =
         sigusr2.recv().await;
 
         // SAFETY: calling into libc, nothing relevant on the Rust side.
+        #[cfg(all(target_os = "linux", target_env = "gnu"))]
         unsafe {
           libc::malloc_trim(0);
         }
